#include <stdio.h>

// declarando variáveis no programa
// não posso utilizar números no início da variável
// case sensitive [f, F]
// nomes de variáveis significativos
// Não posso usar palavras reservadas

int main(){
// variáveis começadas com letras
	int idade = 25; // int vai admitir um valor inteiro 
	int quantidade = 1;
	float altura = 1.75; // float ponto flutuante (casas decimais)
	double peso = 90.3; // double assume valores 
 	char letra = 'a'; // caracters 
	char nome[20] = "matheus";
// variáveis começadas com _
	int _teste;

}

/* tipos de dados ( vai definir os valores que a variável
vai armazenar ) */
